# DSH Studio 体验升级与文案品牌规范化 测试与验证计划 (Test Spec)

> **文档版本**：v1.0.0  
> **责任角色**：QA / DEV  

---

## 1. 测试用例设计矩阵 (Test Matrix)

| 用例 ID | 测试项 | 前置条件 | 操作步骤 | 预期结果 |
| :---: | :--- | :--- | :--- | :--- |
| **TC-01** | 桌宠关闭彻底销毁 | 桌面伴侣处于开启状态且可见 | 1. 打开「设置 > 小鲸鱼姬」<br>2. 点击「启用小鲸鱼姬桌面伴侣」开关置为「已关闭」 | 1. 按钮显示为灰色「已关闭」<br>2. 页面右下角桌宠立绘、对话气泡、状态胶囊即刻完全消失<br>3. DOM 中 `dsh-pet` 相关容器已完全卸载<br>4. 后台漫步计时器与坐标更新彻底停止，无任何移动漂移 |
| **TC-02** | 桌宠重新开启挂载 | 桌面伴侣处于关闭状态 | 1. 打开「设置 > 小鲸鱼姬」<br>2. 点击开关置为「已开启」 | 1. 按钮显示为蓝色「已开启」<br>2. 页面右下角即刻重新挂载立绘与状态胶囊<br>3. 交互气泡与动画恢复正常 |
| **TC-03** | 预设名称与描述核查 | 无 | 1. 打开「设置 > Agent 预设」<br>2. 检查自定义预设卡片 | 1. 卡片标题正确显示为「南梁模式」<br>2. 描述正确显示为「V4 Pro 专属，小南梁进化，南梁，启动！」 |
| **TC-04** | 插件管理文案核查 | 无 | 1. 打开「设置 > 插件」 | 1. 分组卡片标题显示为「Studio 增强插件」<br>2. 描述显示为「统一管理 DSH Studio 全栈增强插件套件的启用与个性化配置。」<br>3. 全局搜索「dsh-web-ui」在 UI 渲染层结果为 0 |
| **TC-05** | 社区插件免责底注核查 | 无 | 1. 展开「设置 > 插件 > 社区插件」<br>2. 滑动至底部 | 1. 底部免责声明显示为「条目由贡献者自行登记，与 DSH Studio 的发布内容无关；使用前请自行评估。」 |

---

## 2. 自动化执行与回归命令

```bash
# 1. 运行核心插件的自动化单测
pnpm --filter dsh-mascot-pet test
pnpm --filter @linxin666/dsh-client-ui-web-ui-settings test
pnpm --filter dsh-liangshen test

# 2. 全仓类型检查
pnpm run typecheck

# 3. 页面视觉与端到端回归验证 (Playwright)
# 验证设置项文案与桌宠卸载行为
```
